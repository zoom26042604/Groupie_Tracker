<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{.Artist.Name}}</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/static/css/artistPage.css">
    <link href="https://api.mapbox.com/mapbox-gl-js/v3.9.4/mapbox-gl.css" rel="stylesheet">
    <script src="https://api.mapbox.com/mapbox-gl-js/v3.9.4/mapbox-gl.js"></script>
    <script src="/static/js/slide.js"></script>
</head>
<body>
<div class="container">
    <h1>{{.Artist.Name}}</h1>



    <h2 class="section-title">Band Members</h2>
    <div class="artist-jsp">
        <ul class="artist-member">
            {{range .Artist.Members}}
                <li>{{.}}</li>
            {{end}}
        </ul>
        <img src="{{.Artist.Image}}" alt="{{.Artist.Name}}" class="artist-image">
        <div class="info-section">
            <p><strong>First Album:</strong> {{.Artist.FirstAlbum | formatDate}}</p>
            <p><strong>Creation Date:</strong> {{.Artist.CreationDate}}</p>
        </div>
    </div>


    <h2 class="section-title">Tour Date and Locations</h2>

    <div class="slider-container slider-1">
        <div class="slider" id="locationSlider">
            {{range .Locations.Locations}}
                <div class="slide">{{. | formatLocation}}</div>
            {{end}}
        </div>
    </div>

    <!-- Deuxième slider pour les dates -->
    <div class="slider-container slider-2">
        <div class="slider" id="dateSlider">
            {{range .Dates.Dates}}
                <div class="slide">{{. | removeAsterisks | formatDate}}</div>
            {{end}}
        </div>
    </div>

    <div class="location-container">

        <input type="checkbox" id="map-toggle" class="map-toggle-checkbox">


        <label for="map-toggle" class="toggle-map-btn">Afficher la carte</label>

        <div id="mapSection" class="map-section hidden">
            <div id="map">
                <script>
                    let locations = {{ .Locations.Locations }}
                </script>
                <script src="https://api.mapbox.com/mapbox-gl-js/v3.9.2/mapbox-gl.js"></script>
                <script src="https://unpkg.com/@mapbox/mapbox-sdk/umd/mapbox-sdk.min.js"></script>
                <script src="/static/js/map.js"></script>
            </div>
        </div>
    </div>
</div>






</body>
</html>